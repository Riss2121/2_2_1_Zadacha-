// Объявление пакета, в котором расположен сервисный слой приложения
package hiber.service;

// Импорт модели User — сущности, с которой работает сервис
import hiber.model.User;

// Импорт интерфейса List для работы с коллекциями
import java.util.List;

// Интерфейс UserService определяет контракт для бизнес-логики,
// связанной с управлением пользователями.
// Сервисный слой выступает посредником между контроллером (или другим клиентом)
// и уровнем доступа к данным (DAO).
public interface UserService {

    // Метод для добавления нового пользователя в систему.
    // Принимает объект User и делегирует сохранение DAO-слою.
    void add(User user);

    // Метод для получения списка всех пользователей из базы данных.
    // Возвращает неизменяемый или изменяемый список (в зависимости от реализации).
    List<User> listUsers();

    // Метод для поиска пользователей по модели и серии их автомобиля.
    // Принимает:
    //   - model: строка с названием модели автомобиля (например, "Camry")
    //   - series: целое число — серия/год выпуска автомобиля (например, 2020)
    // Возвращает список пользователей, у которых автомобиль соответствует заданным параметрам.
    //
    // 🔸 Исправлено форматирование:
    // Было:  List <User>userBySeriesAndModel(...)
    // Стало: List<User> userBySeriesAndModel(...)
    // — пробелы добавлены для соответствия Java Code Conventions.
    List<User> userBySeriesAndModel(String model, int series);
}